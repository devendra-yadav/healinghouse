spring:
  application:
    name: healinghouse

  datasource:
    url: jdbc:mysql://localhost:3306/healing_house_clinic_preprod?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true
    username: hh_user_preprod
    password: ${HEALING_HOUSE_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        #dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false

server:
  port: 9824